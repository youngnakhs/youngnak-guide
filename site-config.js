/* ============================================================
   영락고 진로·대입 정보센터 — 공통 설정 (단일 수정 지점)
   매년 수능 일정 발표 후 이 파일만 갱신하면 됩니다.
   사용법: <script src="site-config.js"></script> 로드 후
           window.YRGO_CONFIG 에서 값 참조 (각 페이지에 폴백 내장)
   ============================================================ */
window.YRGO_CONFIG = {
  /* 다음 수능 시행일 (YYYY-MM-DD) */
  suneungDate: '2026-11-19',

  /* 다음 수능의 학년도 라벨 — D-Day 문구 등에 사용 */
  suneungLabel: '2027학년도 수능',

  /* 현재 고3이 치르는 대입 학년도 */
  admissionYear: 2027,

  /* 2028 대입(현 고2부터)·5등급제 적용 시작 학년도 */
  reformYear: 2028,
};
